[phases.setup]
nixPkgs = ["python313", "python313Packages.pip"]

[phases.install]
cmds = ["python3 -m pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "python scripts/reset_railway_database.py || true && gunicorn app:app --bind 0.0.0.0:${PORT:-8080} --timeout 300 --workers 2"
